/*class Aminal {

    constructor("nombre", "color", "comida"); {
        this.nombre = nombre
        this.color = color
        this.comida = comida
    }
}


const perro = new Aminal("Huesos", "Café", "carne")
const gato = new Aminal("Huesos", "Café", "carne")*/

/*class Mascota {
    constructor("tortuga", "pez", "iguana") {
        this.comida = comida;
        this.sonido = sonido;
    }
    comer() {
        return `comiendo $(this.comida)`
    }
}

class alimento extends Mascot {

    constructor("verduras", "plantas", "insectos") {
        super("verduras", "plantas", "insectos");
        this.comida = 2kg
        }
    comiendo () {
        return `comiendo $(this.comida)`
    }
}

class sonido extends Mascot {

    constructor(chillido, ronquido, silencio) {
        super(chillido, ronquido, silencio);
        this.sonido = diversos
        }
    sonido () {
        return `comunicandose $(this.sonido)`
    }
}
*/

